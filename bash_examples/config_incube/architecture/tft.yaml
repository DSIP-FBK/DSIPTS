# @package _global_

model:
  type: 'tft'
  retrain: true
ts:
  name: 'test'
  version: 1
  enrich: ['dow','hour']
  use_covariates: false

model_configs:
  d_model: 128
  d_head: 32
  n_head: 6
  num_layers_RNN: 12
  optim: torch.optim.Adam
  dropout_rate: 0.5
  persistence_weight: 0.010
  loss_type: 'l1'


train_config:
  batch_size: 64
  max_epochs: 500
            
