# @package _global_

model:
  type: 'persistent'
  retrain: true

ts:
  name: 'weather'
  version: 1
  enrich: []
  silly: false
split_params:
  shift: 0

train_config:
  batch_size: 256
  max_epochs: 0
  auto_lr_find: false #care                 
